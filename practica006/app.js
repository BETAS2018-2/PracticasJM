const archivo = require("./archivo");
const yargs = require("yargs").argv;
const colors = require('colors');

console.log(argv);

let verbo = argv._[0];

switch(verbo){
    case "agregar":
        let alumno = argv.a;
}